{
  "name": "focus-task",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cd extension && gnome-extensions pack --force --out-dir ../dist --extra-source activityModel.js --extra-source topBarViewModel.js .",
    "install:local": "npm run build && gnome-extensions install --force dist/focus-task@yalovyy.com.shell-extension.zip",
    "test": "node --test tests/*.test.mjs"
  }
}
